# Personal AI Development Space

All rules and conventions for this environment live in the **startup skill** (`~/.claude/commands/startup.md`).

**Every session must begin by invoking the `startup` skill.** Do not answer questions, investigate, or run anything until startup is complete.

If you need to add a new rule, add it to `startup.md` — not here.
